📌 Техническое задание: Социальная новостная платформа
1. Общее описание
Разрабатываемая система представляет собой минималистичную социальную сеть новостей. 
Пользователи взаимодействуют с Telegram-ботом, отправляя собственные новости. Каждая новость проходит модерацию и 
в случае одобрения публикуется на веб-сайте. На сайте пользователи могут просматривать новости, ставить лайки и оставлять комментарии.

2. Архитектура проекта
Frontend (веб-интерфейс): Flask-приложение с HTML/CSS (желательно с адаптивным дизайном).

Backend: Python (в том числе Telegram Bot API через aiogram или python-telegram-bot).

Хранение данных: JSON-файлы (альтернатива базе данных).

Хостинг: в разработке

3. Основные модули проекта
3.1 Telegram-бот
Функциональность:

Стартовое сообщение и справка.

Приём новостей от пользователей:

Текст новости (возможно, медиа).

Категория (при необходимости).

Модерация:

Отправка новости админам для проверки.

Принятие/отклонение с кнопками.

Уведомление пользователя о принятии/отклонении.

3.2 Модуль модерации
Функции:

Просмотр ожидающих новостей.

Кнопки "Принять", "Отклонить".

После принятия — сохранение в JSON-файл и отображение на сайте.

3.3 Сайт
Страницы:

Главная:

Лента новостей с фильтрацией по категориям: Свежие, Популярные, Обсуждаемые.

Страница отдельной новости:

Заголовок, текст, дата публикации.

Количество лайков.

Комментарии.

Форма добавления комментария.

Статические страницы: "О проекте", "Контакты" (опционально).

UI/UX:

Простая адаптивная верстка.

Удобный просмотр и взаимодействие с новостями.

3.4 Алгоритмы сортировки
Категории:

Свежие — сортировка по дате добавления (новые сверху).

Популярные — по количеству лайков.

Обсуждаемые — по количеству комментариев.

4. Хранение данных (в планах переделать без базы данных)
*возможная реализация

Формат хранения:
json
Копировать
Редактировать
[
  {
    "id": 1,
    "author_id": 123456,
    "text": "Текст новости",
    "likes": 5,
    "comments": [
      {
        "user_id": 7891011,
        "text": "Комментарий",
      }
    ]
  }
]
Файлы хранения:

news.json — список всех опубликованных новостей.

pending_news.json — новости, ожидающие модерации.

users.json — данные пользователей (если необходимо).

Особенности:

Перед записью в JSON — читать файл, обновлять, сохранять.

Обеспечить блокировку при одновременной записи (например, через threading.Lock или файл-блокировку fcntl в Unix-системах).

5. Лайки и комментарии
Лайки:
Уникальные пользователи могут лайкнуть только один раз.

Лайк добавляется/удаляется при повторном нажатии.

Информация о лайках может храниться отдельно или в списке liked_users в каждой новости.

Комментарии:
Добавление комментариев через форму.

Сохраняются в списке comments каждой новости.

6. Планы на доработку
 Доработка дизайна сайта (адаптивность, читаемость, навигация).

 Реализация системы лайков (в файлах и на фронте).

 Реализация комментариев (на сайте и в JSON).

 Улучшение UX Telegram-бота.

 Возможность редактирования/удаления новостей (в перспективе).

